public enum PlayerStateType { None, Idle, Walk, Run, Underwater, Crouch, No_Input, Dead }

public enum GameStateType
{
    None,
    Play,
    Pause,
    No_Input,
    Init
}

public enum InputType
{
    Keyboard, Xbox, PS5
}
 